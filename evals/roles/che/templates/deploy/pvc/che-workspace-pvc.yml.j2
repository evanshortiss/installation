apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: claim-che-workspace
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
     storage: 10Gi
  storageClassName: "{{ che_persistemt_volume_storageclassname }}"